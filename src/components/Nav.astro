---
import { NAV, SITE } from "../site";

const path = Astro.url.pathname;
const isCurrent = (href: string) =>
  href === "/" ? path === "/" : path.startsWith(href);
---

<header
  class="border-rule bg-paper/90 sticky top-0 z-50 border-b backdrop-blur-md"
>
  <div class="mx-auto flex max-w-7xl items-center gap-6 px-5 py-3.5 sm:px-8">
    <a
      href="/"
      class="flex items-center gap-3"
      aria-label={`${SITE.name} — home`}
    >
      <img
        src="/logo-mark.svg"
        alt=""
        width="44"
        height="35"
        class="h-8 w-auto sm:h-9"
        fetchpriority="high"
      />
      <span class="text-lg leading-none font-bold tracking-tight">
        HAS <span class="text-crimson">Traders</span>
      </span>
    </a>

    <nav aria-label="Main" class="ml-auto hidden lg:block">
      <ul class="flex items-center gap-7">
        {
          NAV.map((item) => (
            <li>
              <a
                href={item.href}
                aria-current={isCurrent(item.href) ? "page" : undefined}
                class:list={[
                  "link-underline text-[0.9375rem] font-medium transition-colors",
                  isCurrent(item.href)
                    ? "text-crimson underline underline-offset-[6px]"
                    : "text-mute hover:text-ink",
                ]}
              >
                {item.label}
              </a>
            </li>
          ))
        }
      </ul>
    </nav>

    <a
      href="/contact/"
      class="bg-crimson hover:bg-crimson-deep ml-auto hidden px-4 py-2.5 text-sm font-medium text-white transition-colors lg:ml-0 lg:block"
      >Start a project</a
    >

    <!-- No-JS disclosure menu for small screens. -->
    <details class="group relative ml-auto lg:hidden">
      <summary
        class="border-rule flex cursor-pointer list-none items-center gap-2 border px-3 py-2 text-sm font-medium [&::-webkit-details-marker]:hidden"
      >
        <span class="group-open:hidden">Menu</span>
        <span class="hidden group-open:inline">Close</span>
      </summary>
      <nav
        aria-label="Main"
        class="border-rule bg-surface absolute right-0 mt-3 w-60 border p-2 shadow-xl"
      >
        <ul>
          {
            NAV.map((item) => (
              <li>
                <a
                  href={item.href}
                  aria-current={isCurrent(item.href) ? "page" : undefined}
                  class:list={[
                    "block px-3 py-2.5 text-sm font-medium",
                    isCurrent(item.href)
                      ? "bg-crimson-wash text-crimson"
                      : "text-mute hover:bg-paper hover:text-ink",
                  ]}
                >
                  {item.label}
                </a>
              </li>
            ))
          }
        </ul>
      </nav>
    </details>
  </div>
</header>
